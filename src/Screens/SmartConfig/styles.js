import {StyleSheet} from 'react-native';
export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    txtIpt: {
        width: 150,
        height: 30,
        borderRadius: 5,
        borderColor: '#e1e1e1',
        borderWidth: 1
    },
    ssidTxtIpt: {

    },
    passwordTxtIpt: {
        marginTop: 10
    }
})
