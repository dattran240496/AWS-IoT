import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    container: {
        width: 50,
        height: 30,
        borderRadius: 8,
        justifyContent: 'center',
        alignItems: 'center'
    },
    title: {
        fontSize: 15,
        color: '#fff'
    }
})
